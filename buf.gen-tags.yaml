version: v1
managed:
  enabled: true
  go_package_prefix:
    default: github.com/cvcio/ministry/internal
    except:
      - buf.build/googleapis/googleapis
plugins:
  - name: gotag
    out: internal
    opt:
      - paths=source_relative